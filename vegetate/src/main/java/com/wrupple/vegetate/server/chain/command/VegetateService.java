package com.wrupple.vegetate.server.chain.command;

import org.apache.commons.chain.Command;

public interface VegetateService extends Command {
	final String REQUEST_SIZE_VARIABLE = "vegetate.requestSize";
}
